export interface User {  id: string;  email: string;  name?: string;  createdAt: Date;  updatedAt: Date;}export interface Product {  id: string;  name: string;  description?: string;  price: number;  stock: number;  imageUrl?: string;  userId: string;  createdAt: Date;  updatedAt: Date;}export type OrderStatus = 'PENDING' | 'CONFIRMED' | 'DELIVERED' | 'CANCELED';export interface OrderItem {  id: string;  orderId: string;  productId: string;  quantity: number;  priceAtOrder: number;  product?: Product; // Populated when including orderItems}export interface Order {  id: string;  customerId?: string;  customerName: string;  customerPhone: string;  customerAddress?: string;  totalAmount: number;  status: OrderStatus;  createdAt: Date;  updatedAt: Date;  userId: string;  orderItems: OrderItem[];}export interface SalesSummary {  todaySales: number;  weekSales: number;  monthSales: number;  conversionRate: number;  avgOrderValue: number;  bestSeller: string | null;  canceledOrders: number;  recentOrders: Order[];}